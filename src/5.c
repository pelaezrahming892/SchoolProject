#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int number = 5;
    double result = sqrt(number);
    printf("The square root of %d is %f\n", number, result);
    return 0;
}
